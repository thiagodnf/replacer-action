name: "Release"

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
      - name: 🛎️ Checkout 
        uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - run: npm install

      - uses: thiagodnf/replacer-action@main
        with:
          find: bar
          replace: foo
          include: |
            src/**, README.md
            test/**

          exclude: |
            .git/**, README.md
            ActionUtils.js