name: "Release"

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: thiagodnf/releaser-action@main
      with:
        find: bar
        replace: foo